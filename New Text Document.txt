MERN Authentication Tutorial - Complete Guide with JWT, Refresh Tokens, 2FA, & More by Small Town Coder


1-> NoSQL Injection

prevent using sanitize -> npm i mongo-sanitize
and zod validation no need to email validation using regex


2-> Email verification

npm i nodemailer
redis ratelimiter for 1 verification mail in min 
(upstash 256Mb free for 1000user approx login->dashboard->create_database-> 
name-> below ioredis-> click eye got redis url)


Ip + email based rate limiting  with Redis
access and refresh token
CSRF
role based authentication 
session authentication



echo "# Authentication" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/sakshi-bargal2000/Authentication.git
git push -u origin main
…or push an existing repository from the command line
git remote add origin https://github.com/sakshi-bargal2000/Authentication.git
git branch -M main
git push -u origin main

 git config --global user.email "you@example.com"
 git config --global user.name "Your Name"

 git config --global user.email "sakshibargal2000@gmail.com"
 git config --global user.name "Sakshi"

  git config --global user.email "testKanchi@gmail.com"
 git config --global user.name "Kanchi"